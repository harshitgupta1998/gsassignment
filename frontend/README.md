# Frontend

This is a dependency-free analyst review UI for the extracted policy rules.

Run it from the repository root so the browser can fetch `backend/output/all_rules.json`:

```bash
python3 -m http.server 8000
```

Then open:

```text
http://localhost:8000/frontend/
```
